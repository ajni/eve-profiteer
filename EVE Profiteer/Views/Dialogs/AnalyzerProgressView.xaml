<Window x:Class="eZet.EveProfiteer.Views.Dialogs.AnalyzerProgressView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Analyzing..." Height="200" Width="300">
    <Grid Margin="30">
        <StackPanel>
            <TextBlock Text="{Binding Status, Mode=OneWay}" />
            <ProgressBar Name="AnalyzerProgressBar" IsIndeterminate="True" Height="20" />
        </StackPanel>
    </Grid>
</Window>